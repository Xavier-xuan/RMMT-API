apiVersion: v1
kind: Service
metadata:
  name: rmmt-student
  namespace: rmmt
  labels:
    app: rmmt-student
spec:
  selector:
    app: rmmt-student
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP